/**
 * ⚠ Placeholder مؤقت — لوحة التحكم نفسها برّه نطاق المهمة الحالية.
 * الغرض من الملف ده إنه يدي وجهة صالحة بعد نجاح تسجيل الدخول بدل ما
 * يوصّل لصفحة مش موجودة.
 */
export function DashboardPlaceholderPage() {
  return (
    <div className="page-head">
      <h1 className="page-title">Dashboard — coming next</h1>
    </div>
  );
}
